<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">
	<select id="getUserInfo" parameterType="hashMap" resultType="com.prac.model.UserModel">
		SELECT
			user_id
			, type
			, user_name
			, user_phone_num
			, DATE_FORMAT(create_dt, '%Y-%m-%d %H:%i') AS create_dt
		FROM 
			user_info
		WHERE
			delete_dt IS NULL
	</select>
	
	<select id="checkUserId" parameterType="String" resultType="Integer">
		SELECT
			COUNT(user_id)
		FROM
			user_info
		WHERE 
			user_id = #{user_id}
			AND delete_dt IS NULL
	</select>
	
	<select id="getUserPassword" parameterType="String" resultType="String">
		SELECT
			user_pwd
		FROM
			user_info
		WHERE 
			user_id = #{user_id}
			AND delete_dt IS NULL
	</select>
</mapper>