<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PostMapper">
	<insert id="setPoster" parameterType="hashMap">
		INSERT INTO board_list
		(
			board_title
			, board_content
			, create_dt
		)
		VALUES
		(
			#{board_title}
			, #{board_content}
			, NOW()
		)
	</insert>
	
	<select id="getPoster" parameterType="hashMap" resultType="com.prac.model.PostModel">
		SELECT
			board_title
			, board_content
			, create_dt
		FROM 
			board
		WHERE
			delete_dt IS NULL
	</select>
</mapper>